import React from 'react';
import {SafeAreaView} from 'react-native';

import {BaseText} from '../styles/baseUI';

const Login = props => {
  return (
    <SafeAreaView>
      <BaseText>teste2</BaseText>
    </SafeAreaView>
  );
};
export default Login;
